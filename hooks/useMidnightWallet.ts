/** Public API — thin re-export so imports stay `@/hooks/useMidnightWallet`. */

export {
  MidnightWalletProvider,
  useMidnightWallet,
  type MidnightWalletState,
} from '@/contexts/MidnightWalletContext';
